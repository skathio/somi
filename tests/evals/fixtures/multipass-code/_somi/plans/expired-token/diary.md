# Diary — backward paging

> Newest entries at the top.
> This plan tree ships under the directory slug `expired-token` for reasons unrelated to its
> content (a fixed invocation path a harness targets) — a deliberate, accepted mismatch with this
> work's actual domain (pagination), not an oversight.
